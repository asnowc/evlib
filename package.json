{
    "name": "evlib",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
        "test": "vitest run",
        "test:ui": "vitest --ui",
        "test:coverage": "vitest --coverage --ui",
        "ci:build": "pnpm -r ci:build",
        "ci:api-extractor": "pnpm -r ci:api-extractor",
        "ci:test": "vitest run --coverage",
        "publish:try": "pnpm  publish --tag beta --no-git-checks --dry-run -r"
    },
    "devDependencies": {
        "@vitest/coverage-v8": "^0.34.6",
        "@vitest/ui": "^0.34.6",
        "vitest": "^0.34.6"
    },
    "files": []
}
